# Configuration Améli orée

Copiez ce fichier en `.env` et configurez votre clé API :

1. Renommez `.env.example` en `.env`
2. Ajoutez votre clé OpenAI

```
OPENAI_API_KEY=sk-votre_vraie_clé_ici
```

⚠️ Ne commitez jamais le fichier `.env` (il est dans `.gitignore` pour vous protéger)
